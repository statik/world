genrule(
    name = "buildifier",
    cmd = "cp $< $@",
    outs = ["buildifier.bin"],
    srcs = ["@com_github_bazelbuild_buildtools_buildifier//file"],
    executable = True,
    visibility = ["//visibility:public"],
)
