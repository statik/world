genrule(
    name = "buildozer",
    cmd = "cp $< $@",
    outs = ["buildozer.bin"],
    srcs = ["@com_github_bazelbuild_buildtools_buildozer//file"],
    executable = True,
    visibility = ["//visibility:public"],
)